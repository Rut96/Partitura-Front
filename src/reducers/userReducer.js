const initialState = {
    userProfile: null,
  };
  
  const userReducer = (state = initialState, action) => {
    switch (action.type) {
      // Handle user-related actions
      default:
        return state;
    }
  };
  
  export default userReducer;
  